/* ======================================================================
   PadTrainer 共通設定
   ── ここを書き換えれば全モードに即反映 ─────────────────────────── */
   const SETTINGS = {
    sensNormal : 20,   // 通常スティック感度
    sensAim    : 10,   // エイム時感度
    playTime   : 60,   // ゲームタイマー（秒）
    hitBoxSize : 0.6,  // 当たり判定スケール（0〜1）
    cursorSize : 50    // ★ カーソル（白円）直径 [px]
  };
  